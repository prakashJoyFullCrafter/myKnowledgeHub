# Memoization & Profiling - Curriculum

## Topics
- [ ] When re-renders are a problem (and when they're not)
- [ ] `React.memo` - skip re-render if props unchanged
- [ ] `useMemo` - memoize expensive computations
- [ ] `useCallback` - stable function references
- [ ] Pitfalls: over-memoization, dependency array bugs, stale closures
- [ ] React Profiler (DevTools) - flamegraph, ranked, commits
- [ ] `<Profiler>` API for programmatic measurement
- [ ] `why-did-you-render` library
- [ ] React Compiler (React 19) - automatic memoization
- [ ] Chrome DevTools Performance panel: flamechart, long tasks
- [ ] Identifying long tasks (> 50ms) blocking main thread
- [ ] `useTransition` and `useDeferredValue` for non-urgent updates
- [ ] Debouncing and throttling user input
- [ ] Avoiding work in render: derive state, don't store it
