# Let, Const & Arrow Functions - Curriculum

## Topics
- [ ] `var` vs `let` vs `const` - scope differences
- [ ] Block scoping with `let` and `const`
- [ ] Temporal Dead Zone (TDZ)
- [ ] `const` with objects and arrays (reference is constant, not value)
- [ ] Arrow functions: `() => {}` syntax
- [ ] Arrow functions and `this` binding (lexical `this`)
- [ ] When NOT to use arrow functions (methods, constructors, `arguments`)
- [ ] Implicit return in arrow functions
