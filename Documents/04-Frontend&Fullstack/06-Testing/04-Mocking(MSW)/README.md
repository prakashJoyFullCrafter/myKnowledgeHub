# Mocking with MSW - Curriculum

## Topics
- [ ] Why mock at the network layer (vs mocking fetch/axios)
- [ ] **MSW** (Mock Service Worker) - intercepts at network boundary
- [ ] Setup for browser (Service Worker) and Node (jest/vitest)
- [ ] Defining handlers: `http.get`, `http.post`, etc.
- [ ] Returning JSON, status codes, headers
- [ ] Dynamic responses based on request body / params
- [ ] Reusing handlers across dev, tests, Storybook
- [ ] Per-test handler overrides with `server.use()`
- [ ] Simulating errors, timeouts, slow networks
- [ ] GraphQL mocking: `graphql.query`, `graphql.mutation`
- [ ] MSW v2 API changes
- [ ] Alternatives: nock (Node-only), Mirage JS
