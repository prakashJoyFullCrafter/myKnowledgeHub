# Python Generators & Iterators - Curriculum

## Topics
- [ ] Iterator protocol: `__iter__()` and `__next__()`
- [ ] `StopIteration` exception
- [ ] Generator functions: `yield` keyword
- [ ] Generator expressions: `(x for x in range(10))`
- [ ] `yield from` for delegating to sub-generators
- [ ] `send()` and `throw()` on generators
- [ ] `itertools`: `chain`, `islice`, `cycle`, `repeat`, `product`, `combinations`, `permutations`
- [ ] `itertools`: `groupby`, `starmap`, `tee`, `zip_longest`
- [ ] Lazy evaluation benefits (memory efficiency)
- [ ] Context managers with `contextlib.contextmanager`
- [ ] `asyncio` generators (async for)
- [ ] Real-world: processing large files, streaming data pipelines

---

## Recommended Practice
| Module | Practice |
|--------|----------|
| Topics | Build a lazy CSV reader that processes millions of rows |
| Topics | Create data pipeline using chained generators |
