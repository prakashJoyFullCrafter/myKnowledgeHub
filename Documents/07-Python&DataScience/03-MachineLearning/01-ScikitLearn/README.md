# Scikit-Learn - Curriculum

## Topics
- [ ] Scikit-Learn API: `fit()`, `predict()`, `transform()`, `score()`
- [ ] Estimator, Transformer, Predictor pattern
- [ ] `Pipeline` - chain preprocessing and model
- [ ] `ColumnTransformer` - different transforms for different columns
- [ ] Preprocessing: `StandardScaler`, `MinMaxScaler`, `LabelEncoder`, `OneHotEncoder`
- [ ] `train_test_split` - splitting data
- [ ] Feature selection: `SelectKBest`, `RFE`
- [ ] `GridSearchCV` and `RandomizedSearchCV` for hyperparameter tuning
- [ ] `cross_val_score` for robust evaluation
- [ ] Saving models: `joblib.dump()`, `joblib.load()`
- [ ] `make_pipeline` and `make_column_transformer` shortcuts

---

## Recommended Practice
| Module | Practice |
|--------|----------|
| Topics | Build end-to-end ML pipeline: load, clean, train, evaluate, save |
